service: my-gcloud-service

provider:
  name: google
  runtime: 'nodejs10'
  project: 'cs3219-otot-task-b'
  credentials: ${env:KEYFILE_PATH}

plugins:
  - serverless-google-cloudfunctions

package:
  exclude:
    - node_modules/**
    - .gitignore
    - .git/**

functions:
  TaskB3:
    handler: app
    events:
      - http: path
